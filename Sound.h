#pragma once

#include <Windows.h>
#include <Mmsystem.h>
#pragma comment(lib, "winmm.lib")
//void playmusic is the function (foward declared)
//The function simply reads a music file .wav and then loops the song for a long duration
void playMusic();